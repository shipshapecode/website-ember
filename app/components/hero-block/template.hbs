{{#responsive-background
  classNames=(concat this.styleNamespace ' ' 'hero'  ' ' @heroClasses)
  image=image
  tagName="page"}}
  <centered>
    {{yield}}
  </centered>

  {{scroll-suggest href=scrollToHref}}
{{/responsive-background}}