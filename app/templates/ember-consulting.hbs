<div itemscope itemtype="http://schema.org/Service">
  {{#hero-block
    heroClasses='ember-consulting'
    scrollToHref='#quote'}}
    <h1 class="hero-header" itemprop="name">Ember Consulting</h1>
  {{/hero-block}}


  <page class="quote" id="quote">
    <centered>
      <box itemprop="review" itemscope itemtype="http://schema.org/Review">
        <h2 class="quoteContent" itemprop="reviewBody">
          “Ship Shape knows what they’re doing.
          A thoughtful approach, pristine code and a 200% effort in everything
          they do, makes recommending them a no-brainer.”
        </h2>
        <br>
        <div itemprop="author" itemscope itemtype="http://schema.org/Person">
          <p class="quoteInfo" itemprop="name">James Dixon</p>
          <p class="quoteInfo" itemprop="jobTitle">Founder of Scout</p>
        </div>
      </box>
    </centered>
    <a data-scroll aria-hidden="true" href="#clients">
      {{scroll-suggest}}
    </a>
  </page>

  <div class="clients" id="clients">
    <centered xs="12">
      <h2 class="section-heading">Clients</h2>
    </centered>
    {{#ember-3d-folding-panel}}
      {{#each model as |item|}}
        {{#item-square item=item}}
          {{img-lazy
            alt=item.heading
            height=100
            src=(concat rootURL item.imageURL)}}
        {{/item-square}}
      {{/each}}
    {{/ember-3d-folding-panel}}
  </div>

  {{contact-footer}}
</div>