{{#each model.files as |post|}}
  <h4>
    {{link-to post.attributes.title 'blog.post' post.attributes.slug}}
    <div>
      By {{post.attributes.author}} {{moment-format post.attributes.date 'LL'}}
    </div>
  </h4>
{{/each}}